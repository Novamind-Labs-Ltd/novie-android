# Mermaid runtime

This directory contains the minified browser bundle for Mermaid 11.16.0 and its MIT license.
The bundle is loaded only through Android's `WebViewAssetLoader`; diagram rendering does not
require network access.

Source: https://cdn.jsdelivr.net/npm/mermaid@11.16.0/dist/mermaid.min.js
